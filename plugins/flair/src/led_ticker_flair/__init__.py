"""led-ticker-flair: homage sprite-trail transitions and text animations.

One distribution, five plugin namespaces (each a `led_ticker.plugins` entry
point): nyancat, pokeball, pacman, sailor_moon (sprite-trail transitions) and
flair (text animations, e.g. animation = "flair.propeller"). The transition
type strings are unchanged from the former per-family packages,
e.g. transition = "nyancat.forward".
"""
